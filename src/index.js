import React from "react";
import ReactDOM from "react-dom/client";
import SensoryMusicApp from "./SensoryMusicApp";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<SensoryMusicApp />);
